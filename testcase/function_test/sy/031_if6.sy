// test if-if-else
int ififElse() {
    int a;
    a = 5;
    int b;
    b = 10;
    if (a != 5)
        if (b != 10)
            a = a + 15;
        else
            a = 25;

    return (a);
}

int main() { return (ififElse()); }
